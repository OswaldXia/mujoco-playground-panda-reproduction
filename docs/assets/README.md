# 教程视觉素材清单

v0.9 复用根 README 已提交的 Mac state render 与 64×64 policy observation。
训练视频和大型图表保留在 ignored artifacts/外部归档，不为装饰而复制进 Git。

v1.0 前需人工选择并压缩：

- [ ] 一条 guide-free 成功回放；
- [ ] 一条 `reached_no_lift` 回放；
- [ ] 一条 `lifted_then_dropped` 回放；
- [ ] original/left/hard 的最终对比图；
- [ ] 图注包含 checkpoint、seed、episode id、协议与是否 guide-free。

素材必须帮助判断机制。只展示最漂亮的成功视频会产生选择偏差，因此失败案例
与完整统计必须同时可追溯。
